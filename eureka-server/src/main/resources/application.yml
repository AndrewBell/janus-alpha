server:
  port: 8761

eureka:
  instance:
    hostname: eureka
    preferIpAddress: true
  client:
    registerWithEureka: false
    fetchRegistry: false

spring:
  application:
    name: eureka
  cloud:
    config:
      discovery:
        enabled: true
